# Программа должна проверить значение переменных
# Обе переменные должны быть чётными
one = 12
two = 30
# Разделим числа с остатком и проверим равен он 0 или нет
# and оба остатка должны равняться 0
print((one % 2 == 0) and (two % 2 == 0))
